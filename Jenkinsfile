pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Testing a Jenkinsfile..'
                /* To test Jenkins pipeline after push */
            } 
        }
    }
}
