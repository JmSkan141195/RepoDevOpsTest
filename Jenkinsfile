pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Testing a Jenkinsfile..'
                /* Testing Jenkins pipeline after pushing */
            } 
        }
    }
}
